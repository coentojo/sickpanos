<h1>Community</h1>


<% @images.each do |image| %>
 	<div><%= link_to image_tag(image.file_url(:thumb), :class => 'small_banner_community'), image_path(image.hashed_url) %></div>
    <br clear="all" />
<% end %>

<br />

<%= link_to 'New Image', new_image_path %>
